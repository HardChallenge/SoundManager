{
    "storage": "string(DIR)",
    "logger": "string(FILE)",
    "restart": true,
    "connection":{
        "host": "string",
        "user": "string",
        "password": "string",
        "database": "string",
        "port": 5000
    }
}